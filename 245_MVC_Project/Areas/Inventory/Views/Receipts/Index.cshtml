@using _245_MVC_Project.HtmlHelpers
@model IEnumerable<ITP245_Model.Receipt>

@{
    ViewBag.Title = "Receipts";
    Layout = "~/Areas/Inventory/Views/Shared/_Layout.cshtml";
}



<p>
    @Html.ActionLink("Create Receipt", "Create", null, new { @class = "btn btn-primary" })
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Quantity)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Amount)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ReceiptDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PoItem.PurchaseOrderNumber)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Quantity)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Amount)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ReceiptDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PoItem.PurchaseOrderNumber)
            </td>
            <td>
                @Html.IndexLinks(item,"Receipts")

                @*@Html.ActionLink("Edit", "Edit", new { id=item.ReceiptId }, new { @class = "label-primary badge " }) |
                    @Html.ActionLink("Details", "Details", new { id=item.ReceiptId }, new { @class = "label-primary badge " }) |
                    @Html.ActionLink("Delete", "Delete", new { id=item.ReceiptId }, new { @class = "label-danger badge " })*@
            </td>
        </tr>
    }

</table>
